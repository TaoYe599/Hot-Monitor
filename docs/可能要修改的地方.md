1. ~~评分体系，硬编码似乎不太好~~ ✅ 已修复 - 评分阈值已提取到 `.env` 配置：`PRE_FILTER_THRESHOLD`, `RELEVANCE_THRESHOLD`, `AUTHENTICITY_THRESHOLD`
2. ~~把总共找到了多少条信息，过滤了多少条等都打印出来方便调试~~ ✅ 已存在 - `scan-runner.ts` 和 `sources.ts` 已有详细日志输出
3. ~~希望能够在热点界面就看到这个帖子的阅读量等信息~~ ✅ 已完成 - `HotspotCard.tsx` 已有 `engagementAggregates` 显示（赞、转、浏览、票、评等）
4. ~~热点在扫描时，点击取消后还是会继续搜索~~ ✅ 已修复 - 在 `sourceService.collect` 中添加了取消检查点
5. ~~有时候AI在摘要里会把我的关键词给翻译了~~ ✅ 已修复 - 在 AI prompt 中添加了「保留英文专有名词」的规则
6. ~~需检查项目的代码文件放置是否合理~~ ✅ 已修复 - 删除了嵌套的 `apps/server/apps/server/` 目录
7. ~~每次前端加了新功能之后，为什么总是没有生效？~~ ✅ 已修复 - 添加了 Service Worker 更新通知机制，检测到新版本后自动提示用户刷新
